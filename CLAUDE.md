This is a jj repo. Use the `jj` skill for any version-control work.
Never run git mutators (commit, branch, checkout -b, reset, push) — use
the jj equivalents.

If the user tells you that you are one of several parallel agents, read
MULTI_AGENT.md from the jj skill before doing anything.
